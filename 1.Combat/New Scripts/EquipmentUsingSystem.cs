using System.Collections;
using System.Collections.Generic;
using System.Security.Cryptography.X509Certificates;
using UnityEngine;

public class EquipmentUsingSystem : MonoBehaviour
{   
    /*
    public EquipmentSystem equipmentSystem;
    public InventoryAllHolder inventoryAllHolder;
    public PlayerMainController playerMainController;

    public double PrimaryWeaponBaseDamage;
    public double PrimaryWeaponTotalDamage;
    public double PrimaryWeaponLevel;
    public int PrimaryWeaponID;

    public double SecondaryWeaponDamage;
    public double SecondaryWeaponBaseDamage;
    public int SecondaryWeaponLevel;

    public int HelmetDamage;
    public int HelmetLevel;

    public int ChestplateDamage;
    public int ChestplateLevel;

    public int LeggingDamage;
    public int LeggingLevel;

    public int BootsDamage;
    public int BootsLevel;

    
    public void ChangePrimaryWeapon(int id)
    {
        PrimaryWeaponID = id;
    }

    void Start()
    {
        
        //inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[0].ID].ItemWeapon.MultiplierDamage

        
        //inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[0].ID].ItemWeapon.MultiplierAmount

        //PrimaryWeaponID
    }

    // Update is called once per frame
    void Update()
    {
        if (PrimaryWeaponID >= 0)
        {
            inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[PrimaryWeaponID].ID].GetDamage(out double NDamage, out long MDamage);
            ///PrimaryWeaponBaseDamage = inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[PrimaryWeaponID].ID].ItemWeapon.AmountDamage;
            //PrimaryWeaponLevel = inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[PrimaryWeaponID].ID].ItemWeapon.NumberAmount + (inventoryAllHolder.InventoryAllItemSystem.InventorySlotWeapons[equipmentSystem.inventorySlotUI[PrimaryWeaponID].ID].ItemWeapon.MultiplierAmount * 1000);

            ///PrimaryWeaponTotalDamage = PrimaryWeaponBaseDamage * (1 + (PrimaryWeaponLevel * 0.2) - 0.2);
            //playerMainController.WeaponAttack = PrimaryWeaponTotalDamage;
        }
    }
    */
}
